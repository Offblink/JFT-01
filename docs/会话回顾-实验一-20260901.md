# 会话回顾：《JAVA框架技术(一)》实验一（2026-09-01）

> 记录原则：只写实际发生并有证据的事。证据 = 命令输出、git 提交、文件存在性。本人（AI 助手）的失误也如实记录。

## 会话时间线

### 阶段一：课程材料梳理
1. 用户要求汇总 3 份 docx（评分表/评分标准/指导书）。officecli 未安装（`command not found`），改用 python-docx（已预装）提取全文。产出：结构化汇总 + 红线提示（AI 抄袭 0 分、无 AI 痕迹维度 0 分、隐私违规通报、3 日时限、实验二权重 10/16）。随后按用户要求生成 `C:/tmp/JAVA框架技术(一)_实验与项目要求汇总.md`（11 章节，6.4 KB）。
2. 用户问"下载 git 不就直接命令行吗"→ 澄清 Git for Windows 本身就是命令行。我回答时未先验证就说"需要装"，用户纠正"我早下载了"→ 实测 `git 2.54.0.windows.1`，user.name=Offblink、email 为 QQ 邮箱。**教训：该先查再答。**

### 阶段二：环境探查
3. 读老师课件 HTML（700 行，MyBatis 组件第 1 节）：6 学时 5 阶段任务；指出课件未给建表 SQL、与指导书的 emp 表/Log4j 差异、LoginInterceptor JSON 转义错误、指导书 `@TableLogic` 误用 status 字段、分页插件 XML 写法过时等问题。
4. 用户决定**不装 AI 插件**（"有你就够了"）→ 我提示评分风险（AI 插件安装是评分要点、AI 痕迹 20 分），用户坚持，改为留痕策略。
5. 找到 IDEA 2026.1（`C:\Program Files\JetBrains\`）；确认 Maven 3.9.11 可用但全局/用户 settings.xml 均无镜像、本地仓库 `~/.m2/repository` 已在使用 → 我创建了 `C:\Users\37549\.m2\settings.xml`（阿里云镜像 `mirrorOf=*`），`mvn help:effective-settings` 实测走 aliyunmaven 成功。
6. 用户质疑"有代理为什么要镜像"→ 事实回答：Java 不读 Windows 系统代理也不读 HTTP_PROXY，对 mvn 而言代理不生效；两者不冲突。

### 阶段三：建项目与 Git 观念
7. 逐项回答新建项目对话框问题：建 Git 仓库=要、模板=不选、Tomcat=不用（实验二才要）、构建系统=Maven、生成器=全跳过。
8. 用户担心 push 污染 GitHub → 说明 commit 纯本地、无 remote 则 push 无门（建议 `git remote -v` 验证）；后续讨论确认 push 会推整条历史（用户正需要此作为过程证据）。

### 阶段四：MySQL 排障（两次）
9. IDEA 报 `Connection refused` → 实查：MySQL Server 9.3 已装（`C:\Program Files\MySQL\MySQL Server 9.3`）但数据目录不存在、无 Windows 服务、3306 无监听 → 判定 Configurator 未跑完，建议用户跑配置向导。
10. 用户称 CLI 密码验证通过但 IDEA 连不上 → 复查：3306 已监听（PID 28252），发现**双 mysqld 进程**（16348 无监听端口的僵尸）；给出 `allowPublicKeyRetrieval` 等三个候选原因 → 用户确认连接成功。双进程问题留待重启收敛。

### 阶段五：项目初始化（用户在 IDEA 建好 JavaEE web 模板后我接手）
11. 用户给 `C:/Users/37549/IdeaProjects/mybatis01`（IDEA 已生成 war 模板 + .git + .gitignore，无提交无远端）。我完成：
    - 重写 pom：groupId `com.offblink`，依赖瘦身为 5 个（servlet-api/mybatis/mysql-connector-j/junit4/log4j），删模板 JUnit 5；
    - 删模板包 org.example，建 `com.offblink.{entity,mapper}` + User/UserMapper + db.properties（密码占位 CHANGE_ME）+ mybatis-config（LOG4J+驼峰+typeAliases）+ 修正版 log4j.properties + mapper/UserMapper.xml + MybatisTest + sql/user_db.sql（建库 mybatis_db，修老师尾逗号）；
    - `mvn compile` 通过；首次提交 `ffc6928`。
12. 用户报 `TypeTag :: UNKNOWN` → 判定旧 Lombok 注解处理器与新版 JDK 不兼容（运行命令显示 jdk-24）；Lombok 本就未被使用 → 从 pom 移除，编译通过。（此步 edit 工具两次因路径/标签问题失败重试，最终成功。）
13. 用户 JUnit 运行日志显示 `jdk-24\bin\java.exe` + `ClassCircularityError` → 指导切换 Project SDK；用户问"能用 21 吗"→ 我建议 8/11（指导书口径）；用户转达**老师推荐 21** → 我尝试 winget 装 Temurin 21，被用户中断（"删了呀，我有了"）；全盘查 `Program Files\Java`、Adoptium、`~/.jdks`、Zulu/Corretto 等均无 21，如实告知未找到，用户未再给路径。

### 阶段六：吸收老师新文件
14. 老师又给 UserMapper.xml + UserMapperTest.java。参考价值：@Before 复用工厂、完整 CRUD、commit 演示、字符串直调 vs 代理对比。发现的问题：引用未定义的 `findByUsernameLike`、硬编码 id=1 且字母序删除先于更新（更新必 0 行）、`createTime(Date)` 与我们实体不符、同一 SQL 双份别名。合入：接口扩到 6 方法、XML 补 LIKE（`#{}` 防注入）、新写 `UserMapperTest`（生命周期测试用回填 id）、删旧 MybatisTest；`mvn test-compile` 通过，提交 `27320d2`。
15. 用户问"为啥还是 JUnit 4"→ 老师给的测试 import `org.junit.*`（4 的标志），跟材料走；两套混用会 import 错乱。

### 阶段七：验证与报告
16. 用户确认 JDK/密码/SQL/git 身份均已解决 → 复核：密码占位符已替换 ✅、git 身份保持 Offblink/QQ（用户决定）、JDK 21 仍未找到但用户已自行处理；`mvn test` 实连数据库三测试全过（5 条查询、回填 id=6 生命周期、LIKE 命中）。控制台中文乱码（GBK vs UTF-8）如实记录，给出 IDEA 编码设置方案。
17. SQL 日志原本只在控制台 → log4j.properties 加 RollingFileAppender，实跑生成 `logs/mybatis.log`（6 条 Preparing），logs/ 加入 .gitignore；按附录 B 写 `docs/实验报告-实验一-草稿.md`（六部分，含两个真实问题 TypeTag/ClassCircularityError）；提交 `d3fdd62`。

### 阶段八：日志栈迁移（用户主导的安全决策）
18. 用户提起 Log4j 漏洞 → web 搜索核实：Log4Shell（CVE-2021-44228）只影响 2.x；1.x（2015 年已 EOL）自有 CVE-2022-23305（JDBCAppender SQL 注入）、23302/23307、2019-17571、2021-4104，均需特定 Appender 触发，本项目当时配置不受影响。
19. 用户要换主流方案 → 对比 reload4j/Log4j2/Logback 后选 **SLF4J + Logback 1.2.13**（指导书原文允许"或 Logback"，老师也附了 logback.xml；1.2.13 兼容 JDK8 且含 CVE-2023-6378 修复）。执行：pom 换坐标、`logImpl=SLF4J`（**中途编辑失误产生重复 logImpl 行，随即修正**）、删 log4j.properties、写 logback.xml（修正老师无效 logger 名 `org.mybatis`、启用滚动文件输出）、实跑验证 12 条 Preparing/Parameters 行，提交 `6f2baca`。
20. 报告草稿同步 5 处（环境表/目标/依赖/配置说明含安全迁移段/AI 记录提示词 3），提交 `c7a9a43`。

### 阶段九：交接
21. 按 handoff 技能生成交接文档：`C:\Users\37549\AppData\Local\Temp\handoff-mybatis01-20260901.md`。

### 阶段十：推送上线
22. 用户提交学习笔记注释（`84e16b9`）；随后发现注释中空的 `#{}` 使 MyBatis 解析崩溃（`StringIndexOutOfBoundsException`——MyBatis 解析占位符先于 SQL 注释语义）→ 改写注释表述规避，提交 `c106239`。
23. 测试报 `Public Key Retrieval is not allowed`（MySQL 8/9 caching_sha2 认证在非 SSL 连接上的已知行为）→ JDBC URL 增加 `allowPublicKeyRetrieval=true`，提交 `fd78867`，三测全绿。
24. gh keyring 中 token 失效 → 设备码流程重新授权（首次尝试未完成，第二次成功）。
25. 创建公开仓库 `github.com/Offblink/JFT-01` 并推送 master 全部提交，本地/远端一致；GitHub 列表由用户自行操作。

## 最终状态（推送时核实）

- Git 8 个提交：`ffc6928` init → `27320d2` CRUD → `d3fdd62` 报告草稿 → `6f2baca` 日志迁移 → `c7a9a43` 报告同步 → `84e16b9` 学习笔记 → `c106239` 注释修复 → `fd78867` JDBC 修复。
- 远端：`origin = github.com/Offblink/JFT-01`（Public），master 跟踪 origin/master，工作区干净，本地/远端一致。
- db.properties 已设 skip-worktree：仓库内为占位配置，本地文件为运行所需配置，二者有意不同（`git ls-files -v` 显示 S 标记）。
- 测试：真实连库三测全过；SQL 日志 `logs/mybatis.log` 持续落盘。
- 报告草稿：六部分齐全，待用户补截图、个人信息、AI 记录真实化。

## AI 助手的失误清单（如实）

1. 未验证就断言"Git 需要安装"（用户早已装好）；
2. edit 工具两次用错路径/标签被拒；
3. logImpl 迁移时产生过一行重复设置（当轮修正）；
4. winget 装 JDK 被用户中断（用户已有/自解决）；
5. 交接文档写"4 个提交"，实际当时已 5 个（本回顾核实为 5）。
